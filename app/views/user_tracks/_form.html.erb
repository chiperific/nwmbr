<%= form_with(model: user_track, local: true) do |form| %>
  <% if user_track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_track.errors.count, "error") %> prohibited this user_track from being saved:</h2>

      <ul>
      <% user_track.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_track_user_id %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.text_field :role, id: :user_track_role %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
